import React from "react";
import { formatDistanceToNow, parseISO } from "date-fns";

export default function Card({ post, onClick }) {
 
  // See examples/Card.html for what this component should render. 
  return <aside></aside>

}
